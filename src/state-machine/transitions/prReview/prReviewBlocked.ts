import { Transition } from '../../types.js';

/**
 * PR Review Phase Blocked Transitions
 * Maps to: "PR Review Phase Blocked Transitions" table in state-machine.md
 */
export const prReviewBlocked: Transition[] = [
  // PR review phase blocked transitions will be implemented here
  // Currently empty - transitions to be implemented based on documentation table
];