import { prReviewBlocked } from './prReviewBlocked';

describe('pr Review Blocked', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(prReviewBlocked).toBeDefined();
  });
});
