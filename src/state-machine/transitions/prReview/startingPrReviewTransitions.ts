import { Transition } from '../../types.js';

/**
 * Starting PR Review (Reparo) Transitions
 * Maps to: "Starting PR Review (Reparo) Transitions" table in state-machine.md
 */
export const startingPrReviewTransitions: Transition[] = [
  // Starting PR review (Reparo) transitions will be implemented here
  // Currently empty - transitions to be implemented based on documentation table
];