import { startingPrReviewTransitions } from './startingPrReviewTransitions';

describe('starting Pr Review Transitions', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(startingPrReviewTransitions).toBeDefined();
  });
});
