import { prReviewTransitions } from './prReviewTransitions';

describe('pr Review Transitions', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(prReviewTransitions).toBeDefined();
  });
});
