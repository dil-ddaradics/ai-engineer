import { prReviewConfirmationTransitions } from './prReviewConfirmationTransitions';

describe('pr Review Confirmation Transitions', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(prReviewConfirmationTransitions).toBeDefined();
  });
});
