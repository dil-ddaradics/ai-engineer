import { contextGatheringBlocked } from './contextGatheringBlocked';

describe('context Gathering Blocked', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(contextGatheringBlocked).toBeDefined();
  });
});
