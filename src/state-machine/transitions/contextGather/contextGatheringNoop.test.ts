import { contextGatheringNoop } from './contextGatheringNoop';

describe('context Gathering Noop', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(contextGatheringNoop).toBeDefined();
  });
});
