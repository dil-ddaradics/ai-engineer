import { contextGatheringTransitions } from './contextGatheringTransitions';

describe('context Gathering Transitions', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(contextGatheringTransitions).toBeDefined();
  });
});
