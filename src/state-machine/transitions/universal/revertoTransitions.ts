import { Transition } from '../../types.js';

/**
 * Reverto Transitions (Exit PR Review)
 * Maps to: "Reverto Transitions (Exit PR Review)" table in state-machine.md
 */
export const revertoTransitions: Transition[] = [
  // Reverto transitions (exit PR review) will be implemented here
  // Currently empty - transitions to be implemented based on documentation table
];