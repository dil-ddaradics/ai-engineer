import { Transition } from '../../types.js';

/**
 * Blocked Finite Transitions
 * Maps to: "Blocked Finite Transitions" table in state-machine.md
 */
export const finiteBlocked: Transition[] = [
  // Blocked finite transitions will be implemented here
  // Currently empty - transitions to be implemented based on documentation table
];
