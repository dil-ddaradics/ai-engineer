import { revertoTransitions } from './revertoTransitions';

describe('reverto Transitions', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(revertoTransitions).toBeDefined();
  });
});
