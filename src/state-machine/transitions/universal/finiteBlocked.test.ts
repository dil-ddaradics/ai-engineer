import { finiteBlocked } from './finiteBlocked';

describe('finite Blocked', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(finiteBlocked).toBeDefined();
  });
});
