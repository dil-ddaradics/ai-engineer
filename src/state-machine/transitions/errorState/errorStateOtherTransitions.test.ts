import { errorStateOtherTransitions } from './errorStateOtherTransitions';

describe('error State Other Transitions', () => {
  // TODO: Delete this dummy test when real tests are implemented
  it('should have transitions defined', () => {
    expect(errorStateOtherTransitions).toBeDefined();
  });
});
